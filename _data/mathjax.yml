loader:
  load:
    - '[tex]/physics'

tex:
  packages:
    '[+]':
      - 'physics'
  inlineMath:
    - ['$', '$']
    - ['\\(', '\\)']
  macros:
    'ord': '\\operatorname{ord}'
    'sgn': '\\operatorname{sgn}'
